<%= render "layouts/errors", :object => @image %>

<%= form_for @image, url: images_path, html: { multipart: true } do |form| %>
  <%= form.label :paperclip, "New Image" %>
  <%= form.file_field :paperclip %>
  <%= form.submit %>
<% end %>
